---
import { loadNews } from '../lib/news';

const news = await loadNews(3);

const repositories = [
  { layer: 'HOST', name: 'SagaSmith-agent', desc: '身份、会话、多渠道与 MCP 编排', descEn: 'Identity, sessions, channels, and MCP orchestration', status: 'ALPHA', url: 'https://github.com/SagaSmithAI/SagaSmith-agent' },
  { layer: 'MCP', name: 'SagaSmith-dnd-mcp', desc: 'D&D 能力服务与会话级工具暴露', descEn: 'D&D capability service and session exposure', status: 'REFERENCE', url: 'https://github.com/SagaSmithAI/SagaSmith-dnd-mcp' },
  { layer: 'CORE', name: 'sagasmith-core', desc: '分支、知识、导入、规则包与持久化', descEn: 'Branches, knowledge, imports, rule packs, persistence', status: 'ACTIVE', url: 'https://github.com/SagaSmithAI/sagasmith-core' },
  { layer: 'RULES', name: 'sagasmith-dnd', desc: 'D&D 5e 2014/2024 规则与结构化战斗', descEn: 'D&D 5e 2014/2024 rules and structured combat', status: 'ACTIVE', url: 'https://github.com/SagaSmithAI/Sagasmith-dnd' },
  { layer: 'RULES', name: 'sagasmith-coc', desc: 'CoC 7e 检定、SAN、追逐与模组解析', descEn: 'CoC 7e checks, sanity, chases, and scenarios', status: 'ACTIVE', url: 'https://github.com/SagaSmithAI/Sagasmith-coc' },
  { layer: 'SKILLS', name: 'SagaSmith-dnd-skills', desc: 'D&D 主持、战役管理与参考资料', descEn: 'D&D GM operations, campaigns, and references', status: 'MCP-FIRST', url: 'https://github.com/SagaSmithAI/SagaSmith-dnd-skills' },
  { layer: 'SKILLS', name: 'SagaSmith-coc-skills', desc: 'CoC 守秘、调查团与场景工作流', descEn: 'CoC Keeper, campaign, and scene workflows', status: 'CLI-FULL', url: 'https://github.com/SagaSmithAI/SagaSmith-coc-skills' },
  { layer: 'CREATE', name: 'SagaSmith-module-gen-skills', desc: '25 种范式的结构化冒险生成', descEn: 'Structured adventure design across 25 paradigms', status: 'SKILL', url: 'https://github.com/SagaSmithAI/SagaSmith-module-gen-skills' },
  { layer: 'UI', name: 'sagasmith-dnd-ui', desc: '面向开团现场的 D&D DM 工作台', descEn: 'A D&D DM workbench for live sessions', status: 'ALPHA', url: 'https://github.com/SagaSmithAI/sagasmith-dnd-ui' },
  { layer: 'UI', name: 'sagasmith-coc-ui', desc: 'CoC Keeper 客户端探索', descEn: 'CoC Keeper client exploration', status: 'PROTOTYPE', url: 'https://github.com/SagaSmithAI/sagasmith-coc-ui' },
  { layer: 'UI', name: 'sagasmith-ui', desc: '跨系统客户端探索', descEn: 'Cross-system client exploration', status: 'PROTOTYPE', url: 'https://github.com/SagaSmithAI/sagasmith-ui' },
];
---
<!doctype html>
<html lang="zh-CN" data-lang="zh">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content="SagaSmithAI 是 AI 原生 TTRPG 平台：可执行规则、分支感知记忆、角色知识边界、结构化规则书与模组导入，以及可供不同 Agent 使用的 MCP 能力服务。" />
    <meta name="theme-color" content="#171a18" />
    <meta property="og:title" content="SagaSmithAI — AI-native TTRPG platform" />
    <meta property="og:description" content="Executable rules. Persistent worlds. Scoped knowledge. Agents that can stay at the table." />
    <meta property="og:type" content="website" />
    <meta property="og:image" content="https://sagasmithai.github.io/logo.png" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>SagaSmithAI — AI 原生 TTRPG 平台</title>
  </head>
  <body>
    <div class="grain" aria-hidden="true"></div>

    <header class="topbar">
      <a class="brand" href="#top" aria-label="SagaSmithAI home">
        <span class="brand-mark">S</span>
        <span>SagaSmith<span>AI</span></span>
      </a>
      <nav aria-label="Primary navigation">
        <a href="#native"><span class="zh">为何原生</span><span class="en">Why native</span></a>
        <a href="#flow"><span class="zh">开团流程</span><span class="en">Campaign flow</span></a>
        <a href="#architecture"><span class="zh">架构</span><span class="en">Architecture</span></a>
        <a href="#ecosystem"><span class="zh">仓库</span><span class="en">Repositories</span></a>
      </nav>
      <div class="header-actions">
        <button class="language" type="button" aria-label="Switch language"><span data-lang-label>EN</span></button>
        <a class="github-link" href="https://github.com/SagaSmithAI" target="_blank" rel="noreferrer">GitHub ↗</a>
      </div>
    </header>

    <main id="top">
      <section class="hero shell">
        <div class="hero-copy">
          <div class="eyebrow"><span class="pulse"></span> ACTIVE ALPHA · OPEN PLATFORM</div>
          <h1>
            <span class="zh">让世界<br /><em>记住每一次选择</em></span>
            <span class="en">Make every world<br /><em>remember the choice</em></span>
          </h1>
          <p class="lede zh">SagaSmithAI 是 AI 原生 TTRPG 平台。它把 Agent、可执行规则、分支时间线、角色知识与用户拥有的规则书和模组，连接成一条可审计的长期战役运行链。</p>
          <p class="lede en">SagaSmithAI is an AI-native TTRPG platform connecting agents, executable rules, branching timelines, actor-scoped knowledge, and user-owned books into one auditable campaign runtime.</p>
          <div class="hero-actions">
            <a class="button primary" href="https://github.com/SagaSmithAI/SagaSmith-agent"><span class="zh">启动 Agent</span><span class="en">Start the Agent</span> <b>↗</b></a>
            <a class="button secondary" href="https://github.com/SagaSmithAI/SagaSmith-dnd-mcp"><span class="zh">接入 D&D MCP</span><span class="en">Connect D&D MCP</span></a>
          </div>
          <div class="manifesto">
            <span>RULES THAT EXECUTE</span>
            <span>WORLDS THAT BRANCH</span>
            <span>KNOWLEDGE THAT STAYS SCOPED</span>
          </div>
        </div>

        <div class="hero-console" aria-label="SagaSmith runtime preview">
          <div class="console-top">
            <div><i></i><i></i><i></i></div>
            <span>CAMPAIGN RUNTIME / LIVE</span>
            <b>SESSION 07</b>
          </div>
          <div class="phase-rail">
            <div class="phase complete"><small>01</small><strong>LOBBY</strong><span>rules · module · party</span></div>
            <div class="phase active"><small>02</small><strong>PLAY</strong><span>scene · knowledge · checks</span></div>
            <div class="phase"><small>03</small><strong>COMBAT</strong><span>turns · actions · temp map</span></div>
          </div>
          <div class="console-grid">
            <div class="scene-card">
              <small>CURRENT SCENE</small>
              <h3>钟楼下的密室</h3>
              <p>The Chamber Beneath the Bell</p>
              <div class="meter"><span style="width: 68%"></span></div>
              <footer><span>scope / party</span><b>68%</b></footer>
            </div>
            <div class="knowledge-card">
              <small>ACTOR KNOWLEDGE</small>
              <div><span class="avatar pc">V</span><p><b>VARIS</b><br />knows: broken sigil</p><i>03</i></div>
              <div><span class="avatar npc">M</span><p><b>MIRA</b><br />knows: hidden patron</p><i>02</i></div>
              <div><span class="avatar gm">GM</span><p><b>KEEPER VIEW</b><br />sealed facts</p><i>09</i></div>
            </div>
          </div>
          <div class="console-log">
            <span>21:14:06</span><b>exposure</b><p>play.scene + play.resolution loaded</p><em>OK</em>
            <span>21:14:11</span><b>branch</b><p>ancestry verified / ash-vault</p><em>OK</em>
            <span>21:14:18</span><b>ruling</b><p>line of sight requires GM decision</p><em class="warn">WAIT</em>
          </div>
          <div class="console-seal"><img src="/logo.png" alt="" /></div>
        </div>
      </section>

      <section class="signal-strip" aria-label="Platform differentiators">
        <div><strong>01</strong><span class="zh">MCP 服务端暴露</span><span class="en">Server-side MCP exposure</span></div>
        <div><strong>02</strong><span class="zh">分支感知连续性</span><span class="en">Branch-aware continuity</span></div>
        <div><strong>03</strong><span class="zh">PC / NPC 独立认知</span><span class="en">Independent PC / NPC knowledge</span></div>
        <div><strong>04</strong><span class="zh">可审计规则结算</span><span class="en">Auditable rule settlement</span></div>
      </section>

      <section class="section shell" id="native">
        <div class="section-intro">
          <span class="section-index">01 / PLATFORM</span>
          <h2><span class="zh">不只是会说话的角色卡</span><span class="en">More than a talking character sheet</span></h2>
          <p class="zh">AI 原生不是给旧工具加一个聊天框，而是让 Agent 的每次判断都能找到证据、受到边界约束，并把结果写回正确的世界与角色。</p>
          <p class="en">AI-native does not mean bolting chat onto an old tool. Every agent decision should find evidence, respect boundaries, and write back to the right world and actor.</p>
        </div>
        <div class="principles">
          <article>
            <span class="principle-no">A</span>
            <h3><span class="zh">规则可执行</span><span class="en">Executable rules</span></h3>
            <p class="zh">确定的输入由引擎结算：骰式、AC、资源、伤害类型、回合与状态。目标、视线、例外和叙事代价仍由 GM 裁决。</p>
            <p class="en">Engines settle dice, AC, resources, typed damage, turns, and state. Targets, sight lines, exceptions, and narrative cost remain GM rulings.</p>
            <small>PRE-FLIGHT → CHOICE → COMMIT</small>
          </article>
          <article>
            <span class="principle-no">B</span>
            <h3><span class="zh">世界会分支</span><span class="en">Worlds can branch</span></h3>
            <p class="zh">Snapshot DAG、branch checkout 与 revision ancestry 决定哪段历史有效。向量检索只寻找候选，绝不决定时间线真相。</p>
            <p class="en">Snapshot DAGs, branch checkout, and revision ancestry determine valid history. Vector retrieval finds candidates; it never decides timeline truth.</p>
            <small>SNAPSHOT · BRANCH · CONTINUITY</small>
          </article>
          <article>
            <span class="principle-no">C</span>
            <h3><span class="zh">角色各有所知</span><span class="en">Actors know differently</span></h3>
            <p class="zh">每个 PC、NPC 和玩家视角都独立维护。服务端按 principal、role、actor、scope 与 branch 过滤，不靠提示词保密。</p>
            <p class="en">Every PC, NPC, and player view is independent. The server filters by principal, role, actor, scope, and branch—not prompt etiquette.</p>
            <small>PRINCIPAL · ACTOR · VISIBILITY</small>
          </article>
          <article>
            <span class="principle-no">D</span>
            <h3><span class="zh">内容能落地</span><span class="en">Content becomes runtime</span></h3>
            <p class="zh">用户规则书和模组经过 staging、解析、质量报告、索引和规则包校验，保留页码、版本与来源，再进入战役。</p>
            <p class="en">User-owned books and adventures pass through staging, parsing, quality reports, indexing, and rule-pack validation with provenance intact.</p>
            <small>SOURCE · PARSE · VALIDATE · PIN</small>
          </article>
        </div>
      </section>

      <section class="section flow-section" id="flow">
        <div class="shell">
          <div class="section-intro inverse">
            <span class="section-index">02 / CAMPAIGN FLOW</span>
            <h2><span class="zh">从开团前到最后一骰</span><span class="en">From the lobby to the last roll</span></h2>
            <p class="zh">工具不是永久堆在模型上下文里。D&D MCP 根据真实战役阶段，为每个会话开放不同的能力面。</p>
            <p class="en">Tools do not remain piled into model context. D&D MCP exposes a different capability surface to every session based on authoritative campaign phase.</p>
          </div>
          <div class="flow-grid">
            <article class="flow-step">
              <header><span>01</span><b>LOBBY</b><em>PREPARE</em></header>
              <h3><span class="zh">把桌子准备好</span><span class="en">Prepare the table</span></h3>
              <ul class="zh"><li>导入规则书与模组</li><li>车卡、装备与准备法术</li><li>权限、分支与初始知识</li></ul>
              <ul class="en"><li>Import books and adventures</li><li>Build characters and prepare spells</li><li>Set access, branches, and knowledge</li></ul>
              <code>lobby.rules · lobby.characters</code>
            </article>
            <div class="flow-arrow">→</div>
            <article class="flow-step focus">
              <header><span>02</span><b>PLAY</b><em>EXPLORE</em></header>
              <h3><span class="zh">让世界回应选择</span><span class="en">Let the world answer</span></h3>
              <ul class="zh"><li>当前场景与连续性上下文</li><li>调查、社交与非战斗检定</li><li>事件、记忆与 actor knowledge</li></ul>
              <ul class="en"><li>Current scene and continuity</li><li>Investigation, social play, checks</li><li>Events, memory, actor knowledge</li></ul>
              <code>play.scene · play.resolution</code>
            </article>
            <div class="flow-arrow">→</div>
            <article class="flow-step">
              <header><span>03</span><b>COMBAT</b><em>RESOLVE</em></header>
              <h3><span class="zh">明确后再结算</span><span class="en">Resolve after clarity</span></h3>
              <ul class="zh"><li>临时战斗地图与可见状态</li><li>攻击预检、反应与选择窗口</li><li>回合、资源、伤害与 Snapshot</li></ul>
              <ul class="en"><li>Temporary map and visible state</li><li>Attack preflight, reactions, choices</li><li>Turns, resources, damage, snapshots</li></ul>
              <code>combat.observe · combat.actions</code>
            </article>
          </div>
          <div class="flow-note">
            <span>SESSION-SCOPED</span>
            <p class="zh">结束战斗后，combat 工具自动失效并回到 play。不同频道、玩家与战役可以在同一个 MCP 进程上拥有不同 exposure。</p>
            <p class="en">When combat ends, combat tools expire and the campaign returns to play. Different channels, players, and campaigns can hold different exposures on one MCP process.</p>
          </div>
        </div>
      </section>

      <section class="section shell" id="architecture">
        <div class="section-intro split-title">
          <div><span class="section-index">03 / ARCHITECTURE</span><h2><span class="zh">边界清楚，Agent 才可靠</span><span class="en">Reliable agents need clear boundaries</span></h2></div>
          <p class="zh">每一层只拥有它真正应该拥有的状态。这样规则升级、系统扩展、更换 Agent Host 或多人接入时，不需要复制领域逻辑。</p>
          <p class="en">Each layer owns only the state it should. Rules can evolve, systems can expand, and Agent Hosts can change without copying domain logic.</p>
        </div>
        <div class="architecture">
          <div class="arch-layer l1"><span>01</span><div><small>HUMAN SURFACES</small><h3>Players · GM · Channels · UI</h3><p><span class="zh">聊天、控制台与桌面体验</span><span class="en">Conversation, controls, and table experience</span></p></div></div>
          <div class="connector"><i></i><code>trusted principal + session</code></div>
          <div class="arch-layer l2"><span>02</span><div><small>AGENT HOST</small><h3>SagaSmith Agent</h3><p><span class="zh">身份、会话、模型、workspace 与多渠道</span><span class="en">Identity, sessions, models, workspace, and channels</span></p></div></div>
          <div class="connector"><i></i><code>MCP / tools · resources · prompts</code></div>
          <div class="arch-layer l3"><span>03</span><div><small>CAPABILITY PLANE</small><h3>Domain MCP Servers</h3><p><span class="zh">渐进发现、会话暴露、权限、幂等与审计</span><span class="en">Discovery, session exposure, access, idempotency, audit</span></p></div></div>
          <div class="connector"><i></i><code>validated domain operations</code></div>
          <div class="arch-bottom">
            <div class="arch-layer l4"><span>04A</span><div><small>RULE SYSTEMS</small><h3>D&D 5e · CoC 7e · Next</h3><p><span class="zh">规则包、角色 schema、战斗与解析 profile</span><span class="en">Rule packs, character schemas, combat, parsers</span></p></div></div>
            <div class="arch-layer l5"><span>04B</span><div><small>SHARED CORE</small><h3>Campaign Runtime</h3><p><span class="zh">分支、知识、导入、检索与持久化</span><span class="en">Branches, knowledge, imports, retrieval, persistence</span></p></div></div>
          </div>
        </div>
        <div class="boundary-row">
          <div><b>SKILLS ≠ ENGINE</b><span class="zh">流程不冒充结算</span><span class="en">Procedure never impersonates settlement</span></div>
          <div><b>SEARCH ≠ TRUTH</b><span class="zh">检索不决定状态</span><span class="en">Retrieval never owns state</span></div>
          <div><b>AGENT ≠ DATABASE</b><span class="zh">Host 不拥有领域数据</span><span class="en">Hosts do not own domain data</span></div>
        </div>
      </section>

      <section class="section ecosystem-section" id="ecosystem">
        <div class="shell">
          <div class="section-intro split-title">
            <div><span class="section-index">04 / OPEN ECOSYSTEM</span><h2><span class="zh">选择你的入口</span><span class="en">Choose your entry point</span></h2></div>
            <p class="zh">这是一个多仓库平台。每个仓库独立版本与测试，README 明确其所有权、依赖和成熟度。</p>
            <p class="en">This is a multi-repository platform. Every repository versions and tests independently, with explicit ownership, dependencies, and maturity.</p>
          </div>
          <div class="repo-grid">
            {repositories.map((repo) => (
              <a class="repo" href={repo.url} target="_blank" rel="noreferrer">
                <header><span>{repo.layer}</span><em>{repo.status}</em></header>
                <h3>{repo.name}</h3>
                <p class="zh">{repo.desc}</p><p class="en">{repo.descEn}</p>
                <footer>OPEN REPOSITORY <b>↗</b></footer>
              </a>
            ))}
          </div>
        </div>
      </section>

      <section class="section status-section">
        <div class="shell status-grid">
          <div class="status-copy">
            <span class="section-index">05 / STATUS</span>
            <h2><span class="zh">先打通一条真实路径</span><span class="en">Prove one real path first</span></h2>
            <p class="zh">D&D 是当前端到端参考实现：从 Agent 会话 exposure、规则书/模组导入、角色与知识、非战斗到结构化战斗和 Snapshot。CoC 与 UI 正向同一边界演进。</p>
            <p class="en">D&D is the current end-to-end reference: session exposure, rulebook and module ingestion, characters and knowledge, play, structured combat, and snapshots. CoC and the UI are converging on the same boundary.</p>
            <a href="https://github.com/SagaSmithAI/.github/blob/main/profile/README.md"><span class="zh">阅读平台总览</span><span class="en">Read the platform overview</span> →</a>
          </div>
          <div class="maturity">
            <div><span>D&D MCP PATH</span><div><i style="width: 84%"></i></div><b>REFERENCE</b></div>
            <div><span>CORE RUNTIME</span><div><i style="width: 78%"></i></div><b>ACTIVE</b></div>
            <div><span>CoC SYSTEM PATH</span><div><i style="width: 56%"></i></div><b>CONVERGING</b></div>
            <div><span>CLIENT UI</span><div><i style="width: 38%"></i></div><b>ALPHA</b></div>
            <small class="zh">进度条表达相对成熟度，不是发布完成百分比。</small>
            <small class="en">Bars show relative maturity, not release-completion percentages.</small>
          </div>
        </div>
      </section>

      {news.length > 0 && (
        <section class="section shell news-section">
          <div class="section-intro split-title">
            <div><span class="section-index">06 / FIELD NOTES</span><h2><span class="zh">锻造日志</span><span class="en">Forge notes</span></h2></div>
            <p class="zh">架构调整、实测路径与重要边界的简短记录。</p><p class="en">Short notes on architecture, real test paths, and important boundaries.</p>
          </div>
          <div class="news-grid">
            {news.map((item) => <article><time>{item.date}</time><h3>{item.title}</h3><p>{item.content.replace(/[#*`]/g, '').slice(0, 220)}{item.content.length > 220 ? '…' : ''}</p></article>)}
          </div>
        </section>
      )}

      <section class="cta">
        <div class="cta-mark">S</div>
        <div>
          <span>ROLL WITH CONSEQUENCE</span>
          <h2><span class="zh">下一场战役，<br />让世界真的记得。</span><span class="en">For the next campaign,<br />build a world that remembers.</span></h2>
        </div>
        <div class="cta-actions">
          <a class="button primary" href="https://github.com/SagaSmithAI/SagaSmith-agent"><span class="zh">开始构建</span><span class="en">Start building</span> ↗</a>
          <a href="https://github.com/SagaSmithAI"><span class="zh">浏览组织</span><span class="en">Browse the organization</span> →</a>
        </div>
      </section>
    </main>

    <footer class="footer shell">
      <div class="brand"><span class="brand-mark">S</span><span>SagaSmith<span>AI</span></span></div>
      <p>AI-NATIVE TTRPG PLATFORM · OPEN SOURCE · ACTIVE ALPHA</p>
      <div><a href="https://github.com/SagaSmithAI">GitHub</a><a href="https://github.com/SagaSmithAI/.github/blob/main/profile/README.md">Overview</a><a href="https://github.com/SagaSmithAI/.github">Profile</a></div>
    </footer>

    <script>
      const root = document.documentElement;
      const button = document.querySelector('.language');
      const label = document.querySelector('[data-lang-label]');
      const setLanguage = (lang) => {
        root.dataset.lang = lang;
        root.lang = lang === 'zh' ? 'zh-CN' : 'en';
        if (label) label.textContent = lang === 'zh' ? 'EN' : '中';
        localStorage.setItem('sagasmith-language', lang);
      };
      setLanguage(localStorage.getItem('sagasmith-language') || (navigator.language.startsWith('zh') ? 'zh' : 'en'));
      button?.addEventListener('click', () => setLanguage(root.dataset.lang === 'zh' ? 'en' : 'zh'));

      const observer = new IntersectionObserver((entries) => {
        entries.forEach((entry) => entry.isIntersecting && entry.target.classList.add('visible'));
      }, { threshold: 0.12 });
      document.querySelectorAll('.section, .signal-strip').forEach((el) => observer.observe(el));
    </script>
  </body>
</html>

<style is:global>
  :root {
    --ink: #171a18;
    --ink-2: #242824;
    --paper: #f3efe6;
    --paper-2: #e7dfd0;
    --orange: #e65025;
    --orange-dark: #b93618;
    --sage: #7e927b;
    --line: rgba(23, 26, 24, .18);
    --mono: "Cascadia Mono", "SFMono-Regular", Consolas, monospace;
    --serif: "Iowan Old Style", "Palatino Linotype", "Noto Serif SC", Georgia, serif;
    --sans: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", "Noto Sans SC", sans-serif;
  }
  * { box-sizing: border-box; }
  html { scroll-behavior: smooth; background: var(--paper); }
  body { margin: 0; color: var(--ink); background: var(--paper); font-family: var(--sans); line-height: 1.6; overflow-x: hidden; }
  [data-lang="zh"] .en, [data-lang="en"] .zh { display: none !important; }
  a { color: inherit; }
  button, a { -webkit-tap-highlight-color: transparent; }
  .grain { position: fixed; inset: 0; pointer-events: none; z-index: 100; opacity: .14; mix-blend-mode: multiply; background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 180 180' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.9' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='.12'/%3E%3C/svg%3E"); }
  .shell { width: min(1240px, calc(100% - 48px)); margin-inline: auto; }
  .topbar { width: min(1320px, calc(100% - 32px)); height: 72px; margin: 14px auto 0; padding: 0 20px; display: flex; align-items: center; justify-content: space-between; position: relative; z-index: 20; border: 1px solid var(--line); background: rgba(243, 239, 230, .86); backdrop-filter: blur(14px); }
  .brand { display: inline-flex; align-items: center; gap: 10px; font: 800 17px/1 var(--sans); letter-spacing: -.03em; text-decoration: none; }
  .brand > span:last-child > span { color: var(--orange); }
  .brand-mark { display: grid; place-items: center; width: 30px; height: 30px; background: var(--ink); color: var(--paper); font: 700 17px/1 var(--serif); transform: rotate(-2deg); }
  nav { display: flex; gap: 28px; }
  nav a { text-decoration: none; font: 700 11px/1 var(--mono); letter-spacing: .06em; text-transform: uppercase; }
  nav a:hover { color: var(--orange); }
  .header-actions { display: flex; align-items: center; gap: 8px; }
  .language { height: 34px; min-width: 38px; border: 1px solid var(--line); background: transparent; font: 700 11px var(--mono); cursor: pointer; }
  .github-link { height: 34px; display: inline-flex; align-items: center; padding: 0 13px; background: var(--ink); color: white; text-decoration: none; font: 700 11px var(--mono); }

  .hero { min-height: 760px; display: grid; grid-template-columns: .95fr 1.05fr; align-items: center; gap: 70px; padding-block: 72px 90px; position: relative; }
  .hero::before { content: ""; position: absolute; width: 540px; height: 540px; left: -330px; top: 70px; border: 1px solid var(--line); border-radius: 50%; box-shadow: 0 0 0 50px rgba(23,26,24,.025), 0 0 0 100px rgba(23,26,24,.018); }
  .eyebrow, .section-index { font: 700 10px/1 var(--mono); letter-spacing: .15em; color: var(--orange-dark); }
  .eyebrow { display: flex; align-items: center; gap: 9px; margin-bottom: 24px; }
  .pulse { width: 7px; height: 7px; border-radius: 50%; background: var(--orange); box-shadow: 0 0 0 6px rgba(230,80,37,.11); }
  .hero h1 { margin: 0; font: 500 clamp(54px, 6.8vw, 98px)/.88 var(--serif); letter-spacing: -.06em; }
  .hero h1 em { display: inline-block; width: max-content; color: var(--orange); font-size: .67em; font-weight: 500; white-space: nowrap; word-break: keep-all; }
  .hero h1 .en em { font-size: .58em; }
  .lede { max-width: 650px; margin: 30px 0 0; font-size: clamp(16px, 1.4vw, 19px); color: rgba(23,26,24,.72); }
  .hero-actions { display: flex; gap: 10px; margin-top: 34px; }
  .button { min-height: 48px; display: inline-flex; align-items: center; justify-content: center; gap: 12px; padding: 0 20px; border: 1px solid var(--ink); text-decoration: none; font: 750 12px var(--mono); letter-spacing: .03em; transition: .2s ease; }
  .button.primary { color: white; background: var(--orange); border-color: var(--orange); }
  .button.primary:hover { background: var(--orange-dark); border-color: var(--orange-dark); transform: translateY(-2px); }
  .button.secondary:hover { background: var(--ink); color: white; }
  .manifesto { display: flex; flex-wrap: wrap; gap: 8px 20px; margin-top: 34px; color: rgba(23,26,24,.5); font: 700 9px var(--mono); letter-spacing: .08em; }
  .manifesto span::before { content: "◆"; color: var(--orange); margin-right: 7px; }

  .hero-console { min-width: 0; position: relative; padding: 16px; color: #e9e5da; background: var(--ink); border: 1px solid #343a35; box-shadow: 24px 28px 0 var(--paper-2), 24px 28px 0 1px var(--line); transform: rotate(.35deg); }
  .console-top { height: 34px; display: flex; align-items: center; justify-content: space-between; padding: 0 2px 12px; border-bottom: 1px solid rgba(255,255,255,.12); font: 700 9px var(--mono); letter-spacing: .12em; color: rgba(255,255,255,.48); }
  .console-top div { display: flex; gap: 5px; }.console-top i { width: 6px; height: 6px; border-radius: 50%; background: #59615b; }.console-top i:first-child { background: var(--orange); }.console-top b { color: #9bb097; }
  .phase-rail { display: grid; grid-template-columns: repeat(3,1fr); gap: 1px; margin-top: 14px; background: rgba(255,255,255,.1); }
  .phase { min-height: 92px; padding: 14px; background: #202420; display: grid; align-content: space-between; position: relative; }
  .phase small { font: 600 9px var(--mono); color: #667068; }.phase strong { font: 800 13px var(--mono); letter-spacing: .08em; }.phase span { font: 9px var(--mono); color: #7f8981; }.phase.complete strong { color: #9bb097; }.phase.active { background: #2a251f; box-shadow: inset 0 -3px var(--orange); }.phase.active strong { color: #ff805a; }
  .console-grid { display: grid; grid-template-columns: 1.1fr .9fr; gap: 10px; margin-top: 10px; }
  .scene-card, .knowledge-card { min-height: 205px; padding: 17px; border: 1px solid rgba(255,255,255,.1); background: #1c201d; }
  .scene-card small, .knowledge-card > small { font: 700 9px var(--mono); letter-spacing: .1em; color: #707a72; }.scene-card h3 { margin: 34px 0 0; font: 500 23px var(--serif); }.scene-card p { margin: 3px 0 25px; font: 10px var(--mono); color: #687069; }.meter { height: 3px; background: #303631; }.meter span { display: block; height: 100%; background: var(--orange); }.scene-card footer { display: flex; justify-content: space-between; margin-top: 9px; font: 9px var(--mono); color: #6f7971; }.scene-card footer b { color: #d0d7cf; }
  .knowledge-card > div { display: grid; grid-template-columns: 30px 1fr auto; gap: 8px; align-items: center; margin-top: 12px; padding-top: 11px; border-top: 1px solid rgba(255,255,255,.08); }.avatar { width: 28px; height: 28px; display: grid; place-items: center; font: 700 9px var(--mono); background: #303630; }.avatar.pc { color: #ff805a; }.avatar.npc { color: #9bb097; }.avatar.gm { color: #d5c48f; }.knowledge-card p { margin: 0; font: 8px/1.5 var(--mono); color: #6f7971; }.knowledge-card p b { color: #cbd1cb; }.knowledge-card i { font: normal 8px var(--mono); color: #747d76; }
  .console-log { margin-top: 10px; padding: 12px; border: 1px solid rgba(255,255,255,.1); display: grid; grid-template-columns: 55px 60px 1fr auto; gap: 7px; font: 8px/1.8 var(--mono); color: #626b64; }.console-log b { color: #9bb097; }.console-log p { margin: 0; color: #a2aaa3; }.console-log em { color: #9bb097; font-style: normal; }.console-log .warn { color: #ff805a; }
  .console-seal { position: absolute; width: 86px; height: 86px; right: -30px; bottom: -33px; border-radius: 50%; overflow: hidden; border: 4px solid var(--paper); box-shadow: 0 0 0 1px var(--line); transform: rotate(-8deg); }.console-seal img { width: 100%; height: 100%; object-fit: cover; }

  .signal-strip { min-height: 105px; display: grid; grid-template-columns: repeat(4, 1fr); border-block: 1px solid var(--line); background: var(--paper-2); opacity: 0; transform: translateY(16px); transition: .5s ease; }.signal-strip.visible { opacity: 1; transform: none; }.signal-strip div { padding: 28px 26px; display: flex; align-items: center; gap: 15px; border-right: 1px solid var(--line); }.signal-strip div:last-child { border: 0; }.signal-strip strong { color: var(--orange); font: 700 11px var(--mono); }.signal-strip span { font: 700 11px var(--mono); letter-spacing: .03em; }
  .section { padding-block: 116px; opacity: 0; transform: translateY(22px); transition: .65s ease; }.section.visible { opacity: 1; transform: none; }
  .section-intro { max-width: 780px; margin-bottom: 55px; }.section-intro h2 { margin: 18px 0 18px; font: 500 clamp(38px, 5vw, 67px)/.98 var(--serif); letter-spacing: -.045em; }.section-intro p { max-width: 720px; margin: 0; color: rgba(23,26,24,.66); font-size: 17px; }
  .principles { display: grid; grid-template-columns: repeat(2, 1fr); border: 1px solid var(--line); }.principles article { min-height: 310px; padding: 36px; position: relative; border-right: 1px solid var(--line); border-bottom: 1px solid var(--line); }.principles article:nth-child(2n) { border-right: 0; }.principles article:nth-child(n+3) { border-bottom: 0; }.principle-no { position: absolute; right: 28px; top: 26px; color: rgba(23,26,24,.11); font: 500 54px var(--serif); }.principles h3 { margin: 54px 0 14px; font: 600 27px var(--serif); }.principles p { max-width: 500px; margin: 0; color: rgba(23,26,24,.65); }.principles small { position: absolute; left: 36px; bottom: 30px; color: var(--orange-dark); font: 700 9px var(--mono); letter-spacing: .09em; }

  .flow-section { background: var(--ink); color: var(--paper); }.section-intro.inverse p { color: rgba(243,239,230,.58); }.flow-grid { display: grid; grid-template-columns: 1fr 40px 1fr 40px 1fr; align-items: stretch; }.flow-step { min-height: 385px; padding: 28px; border: 1px solid rgba(255,255,255,.16); background: #1c201d; }.flow-step.focus { border-color: rgba(230,80,37,.8); background: #241f1a; box-shadow: inset 0 4px var(--orange); }.flow-step header { display: grid; grid-template-columns: auto 1fr auto; gap: 10px; align-items: center; padding-bottom: 18px; border-bottom: 1px solid rgba(255,255,255,.1); font: 700 10px var(--mono); }.flow-step header span { color: var(--orange); }.flow-step header em { color: rgba(255,255,255,.35); font-style: normal; }.flow-step h3 { margin: 44px 0 24px; font: 500 28px var(--serif); }.flow-step ul { margin: 0; padding: 0; list-style: none; color: rgba(255,255,255,.58); font-size: 14px; }.flow-step li { padding: 7px 0; border-bottom: 1px dotted rgba(255,255,255,.1); }.flow-step li::before { content: "—"; margin-right: 9px; color: var(--orange); }.flow-step code { display: block; margin-top: 34px; color: #91a590; font: 9px var(--mono); }.flow-arrow { display: grid; place-items: center; color: var(--orange); font: 18px var(--mono); }.flow-note { margin-top: 24px; padding: 20px 24px; display: grid; grid-template-columns: 170px 1fr; gap: 20px; border-left: 3px solid var(--orange); background: rgba(255,255,255,.045); }.flow-note span { font: 700 10px var(--mono); color: var(--orange); }.flow-note p { margin: 0; color: rgba(255,255,255,.58); font-size: 13px; }

  .split-title { max-width: none; display: grid; grid-template-columns: 1.2fr .8fr; gap: 80px; align-items: end; }.split-title p { padding-bottom: 6px; }.architecture { max-width: 960px; margin: 0 auto; }.arch-layer { min-height: 105px; display: grid; grid-template-columns: 70px 1fr; align-items: center; border: 1px solid var(--line); background: rgba(255,255,255,.28); }.arch-layer > span { height: 100%; display: grid; place-items: center; border-right: 1px solid var(--line); color: var(--orange-dark); font: 700 10px var(--mono); }.arch-layer > div { padding: 18px 24px; }.arch-layer small { color: rgba(23,26,24,.45); font: 700 9px var(--mono); letter-spacing: .11em; }.arch-layer h3 { margin: 4px 0 1px; font: 600 23px var(--serif); }.arch-layer p { margin: 0; color: rgba(23,26,24,.57); font-size: 12px; }.l3 { background: var(--ink); color: var(--paper); border-color: var(--ink); }.l3 > span { border-color: rgba(255,255,255,.15); }.l3 small, .l3 p { color: rgba(255,255,255,.48); }.connector { height: 64px; display: grid; place-items: center; position: relative; }.connector i { height: 100%; width: 1px; background: var(--line); }.connector code { position: absolute; padding: 3px 9px; background: var(--paper); color: rgba(23,26,24,.5); font: 8px var(--mono); }.arch-bottom { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }.arch-bottom .arch-layer { min-height: 120px; }.boundary-row { margin-top: 48px; display: grid; grid-template-columns: repeat(3,1fr); border-block: 1px solid var(--line); }.boundary-row div { padding: 20px; display: flex; flex-direction: column; align-items: center; border-right: 1px solid var(--line); }.boundary-row div:last-child { border: 0; }.boundary-row b { color: var(--orange-dark); font: 700 10px var(--mono); }.boundary-row span { margin-top: 5px; font-size: 12px; color: rgba(23,26,24,.58); }

  .ecosystem-section { background: var(--paper-2); border-block: 1px solid var(--line); }.repo-grid { display: grid; grid-template-columns: repeat(3, 1fr); border: 1px solid var(--line); }.repo { min-height: 230px; padding: 24px; text-decoration: none; border-right: 1px solid var(--line); border-bottom: 1px solid var(--line); transition: .2s ease; }.repo:nth-child(3n) { border-right: 0; }.repo:nth-last-child(-n+2) { border-bottom: 0; }.repo:hover { background: var(--ink); color: var(--paper); transform: translateY(-3px); box-shadow: 0 10px 0 rgba(23,26,24,.08); }.repo header { display: flex; justify-content: space-between; font: 700 9px var(--mono); color: var(--orange-dark); }.repo header em { font-style: normal; color: rgba(23,26,24,.42); }.repo:hover header em { color: rgba(255,255,255,.4); }.repo h3 { margin: 44px 0 8px; font: 600 20px var(--serif); }.repo p { margin: 0; color: rgba(23,26,24,.6); font-size: 13px; }.repo:hover p { color: rgba(255,255,255,.56); }.repo footer { margin-top: 28px; display: flex; justify-content: space-between; color: rgba(23,26,24,.38); font: 700 8px var(--mono); }.repo:hover footer { color: var(--orange); }
  .status-section { background: #d9d0c1; }.status-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 100px; align-items: center; }.status-copy h2 { margin: 18px 0; font: 500 clamp(38px, 5vw, 65px)/.96 var(--serif); letter-spacing: -.04em; }.status-copy p { color: rgba(23,26,24,.65); }.status-copy a { display: inline-block; margin-top: 14px; color: var(--orange-dark); font: 700 11px var(--mono); text-decoration: none; }.maturity > div { display: grid; grid-template-columns: 145px 1fr 80px; gap: 14px; align-items: center; padding: 17px 0; border-bottom: 1px solid var(--line); font: 700 9px var(--mono); }.maturity > div > div { height: 4px; background: rgba(23,26,24,.12); }.maturity i { display: block; height: 100%; background: var(--orange); }.maturity b { text-align: right; color: rgba(23,26,24,.52); font-size: 8px; }.maturity small { display: block; margin-top: 15px; color: rgba(23,26,24,.45); font: 9px var(--mono); }
  .news-section { padding-bottom: 90px; }.news-grid { display: grid; grid-template-columns: repeat(3,1fr); gap: 18px; }.news-grid article { min-height: 210px; padding: 24px; border: 1px solid var(--line); }.news-grid time { color: var(--orange-dark); font: 700 9px var(--mono); }.news-grid h3 { margin: 26px 0 10px; font: 600 21px var(--serif); }.news-grid p { margin: 0; color: rgba(23,26,24,.56); font-size: 12px; white-space: pre-line; }

  .cta { min-height: 440px; padding: 80px max(24px, calc((100vw - 1240px) / 2)); display: grid; grid-template-columns: 150px 1fr auto; align-items: center; gap: 45px; color: var(--paper); background: var(--ink); position: relative; overflow: hidden; }.cta::after { content: ""; position: absolute; width: 560px; height: 560px; right: -220px; border: 1px solid rgba(255,255,255,.08); border-radius: 50%; box-shadow: 0 0 0 70px rgba(255,255,255,.025), 0 0 0 140px rgba(255,255,255,.015); }.cta-mark { width: 126px; height: 126px; display: grid; place-items: center; border: 1px solid rgba(255,255,255,.18); color: var(--orange); font: 500 80px var(--serif); transform: rotate(-3deg); }.cta > div:nth-child(2) span { color: #91a590; font: 700 9px var(--mono); letter-spacing: .14em; }.cta h2 { margin: 14px 0 0; font: 500 clamp(38px, 5vw, 67px)/.96 var(--serif); letter-spacing: -.04em; }.cta-actions { position: relative; z-index: 1; display: flex; flex-direction: column; gap: 18px; align-items: flex-start; }.cta-actions > a:last-child { color: rgba(255,255,255,.58); font: 700 10px var(--mono); text-decoration: none; }
  .footer { min-height: 120px; display: flex; align-items: center; justify-content: space-between; }.footer p { color: rgba(23,26,24,.46); font: 700 8px var(--mono); letter-spacing: .09em; }.footer > div:last-child { display: flex; gap: 18px; }.footer > div:last-child a { font: 700 9px var(--mono); text-decoration: none; }

  @media (max-width: 1000px) {
    nav { display: none; }.hero { grid-template-columns: 1fr; gap: 70px; }.hero-copy { padding-top: 30px; }.hero-console { width: min(680px, 100%); margin: auto; }.signal-strip { grid-template-columns: 1fr 1fr; }.signal-strip div:nth-child(2) { border-right: 0; }.signal-strip div:nth-child(-n+2) { border-bottom: 1px solid var(--line); }.flow-grid { grid-template-columns: 1fr; gap: 14px; }.flow-arrow { transform: rotate(90deg); height: 22px; }.split-title, .status-grid { grid-template-columns: 1fr; gap: 20px; }.repo-grid { grid-template-columns: 1fr 1fr; }.repo:nth-child(3n) { border-right: 1px solid var(--line); }.repo:nth-child(2n) { border-right: 0; }.repo:nth-last-child(-n+2) { border-bottom: 0; }.cta { grid-template-columns: 110px 1fr; }.cta-actions { grid-column: 2; }
  }
  @media (max-width: 680px) {
    .shell { width: min(100% - 28px, 1240px); }.topbar { width: calc(100% - 16px); margin-top: 8px; height: 60px; padding: 0 12px; }.github-link { display: none; }.hero { padding-block: 60px 70px; min-height: auto; }.hero h1 { font-size: 49px; }.hero-console { padding: 10px; box-shadow: 10px 13px 0 var(--paper-2); transform: none; }.console-top > span { display: none; }.phase { min-height: 84px; padding: 10px; }.phase span { display: none; }.console-grid { grid-template-columns: 1fr; }.knowledge-card { min-height: auto; }.console-log { grid-template-columns: 45px 52px 1fr; }.console-log em { display: none; }.console-seal { width: 64px; height: 64px; right: -5px; }.signal-strip { grid-template-columns: 1fr; }.signal-strip div { border-right: 0; border-bottom: 1px solid var(--line); }.section { padding-block: 82px; }.principles { grid-template-columns: 1fr; }.principles article, .principles article:nth-child(2n), .principles article:nth-child(n+3) { border-right: 0; border-bottom: 1px solid var(--line); }.principles article:last-child { border-bottom: 0; }.flow-note { grid-template-columns: 1fr; }.arch-bottom, .boundary-row, .repo-grid, .news-grid { grid-template-columns: 1fr; }.repo, .repo:nth-child(2n), .repo:nth-child(3n), .repo:nth-last-child(-n+2) { border-right: 0; border-bottom: 1px solid var(--line); }.repo:last-child { border-bottom: 0; }.boundary-row div { border-right: 0; border-bottom: 1px solid var(--line); }.boundary-row div:last-child { border-bottom: 0; }.maturity > div { grid-template-columns: 115px 1fr; }.maturity b { display: none; }.cta { min-height: 520px; grid-template-columns: 1fr; gap: 24px; padding-block: 65px; }.cta-mark { width: 80px; height: 80px; font-size: 50px; }.cta-actions { grid-column: auto; }.footer { padding-block: 30px; flex-direction: column; gap: 24px; align-items: flex-start; }.footer p { line-height: 1.7; }
  }
  @media (prefers-reduced-motion: reduce) { html { scroll-behavior: auto; }.section, .signal-strip, .button, .repo { transition: none; } }
</style>
